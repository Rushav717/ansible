- name: nginx mysql_secure_installation
  hosts: all
  become: yes
  serial: 3
  tasks:
  - name: installing nginx
    ansible.builtin.package:
     name: nginx
     state: present
  - name: running the nginx
    ansible.builtin.service:
     name: nginx
     state: started