#!/bin/bash

- name: filters
  hosts: web
  tasks:
  - name: handle undefined variable
    ansible.builtin.debug:
     msg: "hello {{person | default('RUSHHAV')}}"
  
  - name: Convert string to list
    vars:
     fruits: "apple,banana,orange"
    ansible.builtin.debug:
     msg: "FRUITS ARE {{ fruits | split(,) }}"
    